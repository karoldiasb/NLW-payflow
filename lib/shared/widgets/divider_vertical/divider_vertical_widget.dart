import 'package:flutter/material.dart';

class DividerVerticaWidget extends StatelessWidget {
  const DividerVerticaWidget({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}